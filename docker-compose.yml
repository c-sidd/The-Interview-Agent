version: '3.8'

services:
  interview-agent:
    build: .
    container_name: ai-interview-agent
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
